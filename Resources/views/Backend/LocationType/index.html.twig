{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'WebburzaSyliusLocationBundle:Backend/LocationType/Macros:macros.html.twig' import list %}

{% block topbar %}
    <ol class="breadcrumb">
        <li>{{ 'sylius.ui.assortment'|trans }}</li>
        <li>{{ 'webburza.sylius.location_type.breadcrumb.index'|trans }}</a></li>
    </ol>
{% endblock %}

{% block content %}
    <div class="page-header">
        <div class="actions-menu">
            {{ buttons.create(path('webburza_location_location_type_create'), 'webburza.sylius.location_type.create'|trans) }}
        </div>
        <h1>
            <i class="glyphicon glyphicon-file"></i> {{ 'webburza.sylius.location_type.index_header'|trans|raw }}
        </h1>
    </div>

    {{ pagination(location_types) }}
    {{ list(location_types) }}
    {{ pagination(location_types) }}

{% endblock %}
